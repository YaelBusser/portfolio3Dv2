import "./index.css";
import Header from "../Header";

const Nav = () => {
    return (
        <nav>
            <button>About</button>
            <button>Projects</button>
            <button>Contact</button>
            <Header />
        </nav>
    )
}

export default Nav;
